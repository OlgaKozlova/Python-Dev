# День 36 — Практика

## Задача 1

Предсказать результат и объяснить почему:

```python
a = [1, 2]
b = a
c = [1, 2]

print(a == b)
print(a is b)
print(a == c)
print(a is c)
```

Объяснить каждую строку через модель ссылок.

---

## Задача 2

Что выведет код?

```python
x = 10
y = 10

print(x is y)
```

И почему этот результат нельзя использовать как гарантию поведения?

---

## Задача 3

Почему это плохой стиль?

```python
if value == True:
    print("yes")
```

Как написать корректно и почему?

---

## Задача 4

Объяснить разницу между этими проверками:

```python
if x == None:
    ...

if x is None:
    ...
```

Какая форма правильная и почему?

---

## Контроль понимания

Ответить устно или письменно:

1. Что хранит переменная в Python?
2. Что проверяет `is`?
3. Почему нельзя использовать `is` для чисел?
4. Когда использование `is` корректно?
